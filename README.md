# Repositório do projeto de testes com a React Testing Library!

<details>
  <summary><strong>👨‍💻 O que foi desenvolvido</strong></summary><br />

  - Testes para uma aplicação React que já está criada e configurada, utilizando [`Jest`](https://jestjs.io/) e a biblioteca [`React Testing Library`](https://testing-library.com/).
 
  - A aplicação contém uma implementação completa de todos os requisitos da Pokédex. Para cada requisito listado, testes que garantam sua corretude.
</details>

<details>
  <summary><strong>:memo: Habilidades</strong></summary><br />

  * Utilizar os seletores (queries) da React-Testing-Library em testes automatizados;

  * Simular eventos com a React-Testing-Library em testes automatizados;

  * Testar fluxos lógicos assíncronos com a React-Testing-Library;

  * Escrever testes que permitam a refatoração da estrutura dos componentes da aplicação sem necessidade de serem alterados;

  * Testar inputs.
</details>


## Instalando Dependências

- Clone o projeto:

  ```bash
  git clone git@github.com:luizaogura/luiza-ogura-project-react-testing-library.git
  ```

  > Front-end

  ```bash
  cd luiza-ogura-project-react-testing-library
  npm install
  ```

## Executando Testes

- Para rodar todos os testes:

  ```bash
  npm test
  ```

- Para rodar um teste individualmente vá em `src/tests` e utilize o nome do arquivo ex:

  ```bash
  npm test About.test.js
  ```
